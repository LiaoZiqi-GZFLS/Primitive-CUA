# zhihu-publish-article-with-ai-cover-and-self-engage

Publish a Zhihu column article with an agent-generated cover image, then verify it via search and perform permitted self-interactions (comment, upvote, favorite) while respecting platform self-interaction rules

## Prerequisites

A valid Zhihu account with login credentials or an existing session; prepared article title and body text; local capability to generate and save an image file (SVG/Canvas/text-to-image); browser access to zhihu.com; awareness that Zhihu blocks some self-interactions (喜欢 on own content) so the workflow must tolerate platform-rule rejections

## Steps

1. Log in to Zhihu web with the target account before any content operations
2. Open the column editor (zhuanlan.zhihu.com), enter the article title, and paste the full body text
3. Analyze the article's theme and auto-generate a matching cover image locally (SVG, HTML Canvas, or text-to-image), saving it as a PNG file
4. Insert the image at the start of the body via the editor's image button → local upload → select the generated file → confirm insertion
5. Click publish, wait for the success redirect, and record the article URL (zhuanlan.zhihu.com/p/...)
6. Search the exact article title in Zhihu search; verify it ranks in results with the cover thumbnail, then click through to the article page
7. Post a comment and reopen/refresh the comment panel to confirm it persisted (count increments)
8. Click 赞同 (upvote) and verify the button turns blue/active; click 收藏 (favorite) and confirm the star count increments and the favorites dialog shows it as collected
9. Attempt 喜欢 (like) once; if the platform shows a red '不能对自己的内容互动' toast, recognize it as a hard platform rule, stop retrying, and mark the step as blocked-by-platform rather than failed
10. Save newly discovered platform interaction limits (e.g., which self-interactions are allowed vs blocked) to long-term memory for future runs

## Tools Used

click, set_mouse, type_keys, paste_text, memory, finish, image-generation (SVG/Canvas/text-to-image)

> Auto-generated by CUA AutoSkill v1.0 on 2026-07-24 07:39
