# notepad-type-text

Launch Notepad, focus its window, and insert arbitrary text via paste with verification.

## Prerequisites

Windows desktop with Notepad installed, GUI automation tools (run_command, list_windows, focus_window, paste_text) available, and the text to be entered is known in advance.

## Steps

1. Recall any previously saved Notepad workflow from memory (key: skills/notepad)
2. Launch Notepad with run_command('notepad')
3. Wait ~2 seconds for the application to open
4. Confirm the window exists via list_windows (look for '无标题 - Notepad' or 'Untitled - Notepad')
5. Focus the Notepad window with focus_window using its title
6. Insert the target text with paste_text (paste is more reliable than simulated typing)
7. Verify success: check the tab title reflects the content and/or the status bar shows expected line/column/character count (e.g., 11 chars for 'hello world')
8. Save the proven workflow to memory for future reuse

## Tools Used

memory, run_command, wait, list_windows, focus_window, paste_text, screenshot

> Auto-generated by CUA AutoSkill v1.0 on 2026-07-19 23:11
